<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ma.rest.mapper.dao.ProvinceMapperExtra" >
    <resultMap id="ProvinceResultMap" type="com.ma.rest.base.model.Province" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="provinceid" property="provinceid" jdbcType="VARCHAR" />
        <result column="province" property="province" jdbcType="VARCHAR" />
    </resultMap>
    <sql id="Base_Column_List" >
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        id, provinceid, province
    </sql>
    <select id="selectProvinceList" resultMap="ProvinceResultMap">
        <!--
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        select
        <include refid="Base_Column_List" />
        from provinces

    </select>

    <select id="selectAddressList" resultType="Map" parameterType="java.lang.Integer">
        select
             address_id addressId,user_id userId,province_code provinceCode,city_code cityCode,
             area_code areaCode,address_details addressDetails,is_default isDefault,phone,linkman
        from ma_address
        <where>
            <if test="userId!=null">
                 user_id=#{userId}
            </if>
            <if test="isDefault!=null">
                and is_default=#{isDefault}
            </if>
        </where>
    </select>

    <insert id="addAddress" parameterType="Map">
        insert into ma_address(user_id,province_code,city_code,area_code,address_details,is_default,phone,linkman)
        values (#{userId},#{provinceCode},#{cityCode},#{areaCode},#{addressDetails},#{isDefault},#{phone},#{linkman})
    </insert>

    <update id="updateAddress" parameterType="Map">
        update ma_address
        <set >
            <if test="provinceCode != null" >
                province_code=#{provinceCode},
            </if>
            <if test="cityCode != null" >
                city_code=#{cityCode},
            </if>
            <if test="areaCode != null" >
                area_code=#{areaCode},
            </if>
            <if test="addressDetails != null" >
                address_details=#{addressDetails},
            </if>
            <if test="isDefault != null" >
                is_default=#{isDefault},
            </if>
            <if test="phone != null" >
                phone=#{phone},
            </if>
            <if test="linkman != null" >
                linkman=#{linkman},
            </if>
         </set>
        where address_id = #{addressId,jdbcType=INTEGER}

    </update>
    <!--删除地址-->
    <delete id="deleteAddress" parameterType="java.util.Map">
        delete from ma_address
        <where>
            <if test="userId!=null">
                user_id=#{userId}
            </if>
            <if test="addressId!=null">
                and address_id=#{addressId}
            </if>
        </where>
    </delete>
</mapper>